<h1>Warehouse Locations</h1>

<h3>Click a location to manage that warehouse's inventory. If you don't see your warehouse, you can add it using the button below</h3>

<% @warehouses.each do |warehouse| %>
  <h3><%= link_to warehouse.location, warehouse_path(warehouse.id) %></h3>
<% end %>
<br>
<%= button_to 'Add Warehouse', new_warehouse_path, method: :get %>
<br>
<%= button_to 'Home', root_path, method: :get %>
